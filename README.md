# TailwindCSS Coffee Style by vuongduchieu0602

## Requirements

* **nodejs >= v18.16.0**
* **npm >= 9.5.1**

Clone project and run test:

```
$ git clone ...
$ cd <project dir>
$ npm install
$ npm run build:css
$ npm run start:dev
```
